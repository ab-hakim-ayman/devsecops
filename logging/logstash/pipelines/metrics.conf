input {
  beats {
    port => 5046
  }
}

filter {
  # Example: Convert logs to metrics
  metrics {
    meter => "app_requests"
    add_tag => "metric"
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch.logging.svc.cluster.local:9200"]
    index => "metrics-%{+YYYY.MM.dd}"
  }
}
